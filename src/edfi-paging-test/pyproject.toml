[tool.poetry]
name = "edfi-paging-test"
version = "0.1.0"
description = ""
authors = ["vimayya <vinaya.mayya@ed-fi.org>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
